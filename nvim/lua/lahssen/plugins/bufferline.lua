return {
  "akinsho/bufferline.nvim",
  dependencies = {"nvim-tree/nvim-web-devicons"},
  version = false, --"*",
  opts = { -- require(bufferlines).setup(opts)
    options ={
      mode = "tabs",
      separator_style = "slants",
    }
  }
}
